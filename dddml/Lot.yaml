#%DDDML 0.1

---

# ------------------------------
aggregates:
  
  # ---------------------------------
  # 批次
  Lot:
    id:
      name: LotId
      type: id-ne
      
    properties:
      # 数量
      Quantity:
        type: fixed-point
      # 过期日期
      ExpirationDate:
        type: date-time
      # CreationDate:
      #  type: date-time
        
    # metadata: 
    #   StateEventDatabaseTableName: LOT_STATE_EVENT
