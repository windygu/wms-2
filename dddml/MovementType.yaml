#%DDDML 0.1

---

# ------------------------------
aggregates:
              
    # ----------------------- aggregate -------------------------
    # 库存变化（移动）类型
    MovementType:
      id:
        name: MovementTypeId
        type: string
      properties:
        Description:
          type: string
          notNull: true
      metadata:
        AbsolutelyNoEventSourcing: true

          