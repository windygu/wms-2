#%DDDML 0.1

---

# ------------------------------
services:

  # ---------------------------------------
  # 线索跟进服务
  IdService:
    
    # ------------------------------------
    # 方法
    methods:
      
      # ------------------------------------
      # 获取一个可用的单号
      GetDocumentNumber:
      
        parameters:
          # 单据类型
          DocumentType:
            type: string
            
        # 返回结果
        result:
          type: string
            
            